<%= form_for @changeset, "/signup", [name: :participant], fn f -> %>

  <%= if f.errors != [] do %>
    <div class="alert alert-danger">
      <p>Please check the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= text_input f, :name,
      placeholder: Workshop.I18n.tr(@conn, "form.labels.name") %>
  <%= text_input f, :email,
      placeholder: Workshop.I18n.tr(@conn, "form.labels.email") %>
  <%= text_input f, :occupation,
      placeholder: Workshop.I18n.tr(@conn, "form.labels.occupation") %>

  <%= Workshop.I18n.tr(@conn, "form.english") %>
	<br />
	<label>
    <%= checkbox f, :confirm %>
    <%= Workshop.I18n.tr(@conn, "form.understand") %>
	</label>

  <%= select f, :workshop, [
      {Workshop.I18n.tr(@conn, "form.workshop.default"), nil},
      {Workshop.I18n.tr(@conn, "form.workshop.entrepreneur"), :entrepreneur},
      {Workshop.I18n.tr(@conn, "form.workshop.student"), :student}] %>
  
  <%= submit Workshop.I18n.tr(@conn, "form.signup") %>

<% end %>
